
parameters:
  AzureSubscription:
  ServerName:
  DatabaseName:
  SqlUsername:
  SqlPassword:
  DacpacFile:
  AdditionalArguments:
  EnableAdditionalArguments: false
  EnableSqlAdditionalArguments: false

steps:
- task: SqlAzureDacpacDeployment@1
  displayName: Execute Azure SQL DacpacTask
  inputs:
    AzureSubscription: ${{ parameters.AzureSubscription }}
    ServerName: ${{ parameters.ServerName }}
    DatabaseName: ${{ parameters.DatabaseName }}
    SqlUsername: ${{ parameters.SqlUsername }}
    SqlPassword: ${{ parameters.SqlPassword }}
    DacpacFile: ${{ parameters.DacpacFile }}
    ${{ if eq(parameters.EnableAdditionalArguments, true) }}:
      AdditionalArguments: ${{ parameters.AdditionalArguments }}
    ${{if eq(parameters.EnableSqlAdditionalArguments, true) }}:
      SqlAdditionalArguments: ${{ parameters.SqlAdditionalArguments }}